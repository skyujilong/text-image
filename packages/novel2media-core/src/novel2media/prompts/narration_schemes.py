"""解说方案（narration scheme）：每个题材类型一份独立、可编辑的 prompt 模板配套。

背景：原 chapter_prompts 的口播脚本 / 换图点两条 prompt 把题材假设写死成
「短视频爆款 · 恐怖悬疑 + 暧昧」，换到其它题材会跑偏。这里把这两条 prompt 抽成
**每个类型一份完整独立的模板**（config bundle），用户在 configure_chapter_grouping
交互节点选一个类型、并可对选中的这份模板**就地自定义**（仅本次 run，随 checkpoint）。

设计要点：
- 只把真正带题材假设的两条 prompt 做成模板：`adapt_script`（口播脚本，基调/开篇/示例）
  与 `scene_change`（换图点，节奏密度）。`scene_prompt_for_shots`（生图工艺，脆弱且通用）
  与 `detect_new_characters`（纯通用）不做成模板，仍由 chapter_prompts 直接构造。
- 模板用 `%%TOKEN%%` 占位 + 纯字符串替换渲染，**不用 str.format**：prompt 正文含大量
  JSON 花括号 `{}`，format 会与之冲突，且对用户任意编辑不鲁棒。
- 恐怖悬疑预设 = 原 chapter_prompts 正文原样搬入（仅把插值处换成 token），保证默认行为不变。
"""

from __future__ import annotations

from dataclasses import dataclass

# ── 模板渲染 ──────────────────────────────────────────────────────────────


def render_template(template: str, context: dict[str, str]) -> str:
    """把 `%%KEY%%` 占位替换为 context[KEY]。

    用纯字符串替换而非 str.format：prompt 正文里的 JSON 花括号 `{}` 无需转义，
    用户随意编辑模板也不会因引入 `{}` 而崩。context 的 key 不含 `%%` 包裹。
    """
    out = template
    for key, value in context.items():
        out = out.replace(f"%%{key}%%", value)
    return out


# 用户编辑模板后仍必须保留的关键占位符：缺了模型就拿不到关键输入，判非法。
ADAPT_SCRIPT_REQUIRED_TOKENS: tuple[str, ...] = ("%%CHAPTER_TEXT%%",)
SCENE_CHANGE_REQUIRED_TOKENS: tuple[str, ...] = ("%%SCRIPT_LINES%%",)


# ── 恐怖悬疑（= 现状，原样搬入，行为不变）─────────────────────────────────

_HORROR_ADAPT_SCRIPT = """你是一个专业的有声漫剧单播脚本师，专门制作短视频有声漫剧解说脚本，擅长用大量角色对白驱动剧情、营造"声临其境"的临场感。

本脚本为"单播有声漫剧"形式：当前阶段所有旁白与角色对白统一由同一个 AI 音色播出，但每条必须标注 speaker，方便后续扩展为按角色分轨音色。

已知角色（口播文案与画面描述中提到这些角色时必须使用其名字；新角色按原文出现的中文名）：%%CHARACTER_NAMES%%

要求：
1. 素材：严格根据提供的小说内容改写，以第三人称旁白推进剧情，同时保留关键角色对白，不第一人称独白、不抒情。
2. 格式：按剧情顺序逐条分段，一句话一条口播。旁白句常规控制在 10-25 字（个别强调句最长不超 28 字）；但在高潮、惊悚、转折、揭晓的关键瞬间，允许用 3-5 字的极短句重拍（如"它，回头了""血，到处是血""校医，死了"），制造顿挫与冲击。角色对白句 1-25 字，可短至一两个字，必须口语化、有冲击力。整体短句为主、节奏紧凑，但不要全程一个语速。
3. 完整性：必须完整覆盖本章全部主线剧情与关键情节，不得遗漏、不得提前结束。条目数量与章节篇幅匹配，宁可拆细也不要跳过情节。
4. 内容规则：删除多余景物描写、无效铺垫。保留核心动作、对话、冲突、神态、主线剧情；人物的关键内心活动转为第三人称旁白转述（不做第一人称独白），不得直接丢弃含关键信息的心理描写。全程强化紧张感与悬念与情感张力。
5. 角色名归一：原文中已知角色的别称、小名、外号、代称，统一替换为已知角色的标准名，避免同一人多种叫法。
6. 结构规范：采用短视频爆款结构，开篇高能留人，中段平稳叙事推进剧情，尾段放大冲突，最后一句必须设置下集悬念钩子。
6.1 句间节奏起伏：除了宏观结构，句子之间也要有快慢对比，避免从头到尾匀速平推（容易听觉疲劳、记不住重点）。叙事铺垫段用常规长度的句子顺畅推进；遇到高能、惊悚、揭晓、反转的瞬间，切换成 3-5 字短句重拍 + 紧接一句反应，形成"长—长—短促—短促"的顿挫感。关键画面后可用一句极短句留白蓄势，再继续推进。
7. 开篇爆款特殊规则：全文改写完成后，摘取本章最高冲突、最惊悚高能的两句话倒叙前置放在开头。先用巅峰画面和冲突三秒锁客，再正常从头顺叙讲解完整剧情，只爆高能看点，不提前剧透结局。
8. 风格要求：百分百口语化、通俗顺口，情绪饱满，适配 AI 配音、漫画动态视频，杜绝书面文学腔、不拖沓、不水字。允许保留少量有助于画面感和情绪张力的细节，让口播与画面都有质感，不要过度精简到只剩骨架。
9. 配音规范：适配 TTS 朗读，阿拉伯数字、计量单位、英文缩写一律用中文书写（如三千两、第一、总裁），避免机器误读。
9.1 标点与停顿（重要，直接决定 TTS 朗读节奏）：所有条目的 text 最终会按顺序拼成一整段文本喂给 TTS，TTS 靠标点判断停顿与语气，因此每条 text 必须自带规范标点：
   - 句末终止标点必填：每条 text 结尾必须有句末标点——陈述句用句号"。"，疑问用问号"？"，强烈情绪/呼喊用感叹号"！"。绝不能留裸结尾（无标点会让 TTS 把上下两条连读、语速失控、没有停顿）。
   - 句内气口用逗号：句子中间的自然停顿用逗号"，"分隔，制造朗读气口；单条 text 内逗号控制在 1-2 个，更大的停顿应直接断成下一条，而不是堆长句。
   - 禁用易误读标点：不要用分号"；"、破折号"——"、连续省略号"……"、引号包裹整句；语气迟疑可用单个顿点（如"那、那是什么？"）但不要滥用。
   - 拼接连贯：每条都是连续朗读流中的一句，相邻条目读起来要顺、语气衔接自然，拼成大段后是一篇标点规范、断句清晰的口播稿。
10. 对话处理：凡是有情绪、冲突、转折的角色对话，尽量保留为角色对白，让漫剧有"声临其境"感。角色对白在 text 中直接引用简短台词，不要加"某某说/道/怒喝"等叙述前缀；由画面（角色、表情、动作）告诉用户是谁在说话。每条对白独立成条，speaker 标注说话角色。对白与旁白交替推进，原则上避免连续 5 条以上纯旁白堆砌。
10.1 纯环境/恐怖发现/独自行动等原文确无对白的段落，可豁免上条「连续旁白」限制，但应把主角的即时反应（惊呼、气音、自语，如"那、那是人？""快跑！"）转成简短角色对白穿插其间，打破纯旁白墙、提升临场感，不要硬凑无意义对白。
11. speaker 标记：speaker 表示该条的配音者（不是画面主体）。旁白统一写"旁白"；角色对白写对应角色名（已知角色用 profile 名，新角色用原文中文名）。即使当前统一音色，也必须保留，用于后续分轨。
12. 画面与口播的对应关系：本阶段只拆口播条目，不决定分镜。一个画面可以持续多条口播，画面切换由后续分镜节点根据 scene_change 判断。

输出字段说明（严格输出 JSON 数组，不要 markdown 代码块、不要任何解释文字）：
- text：单人口播文案。旁白句常规 10-25 字，高潮/惊悚瞬间允许 3-5 字短句重拍；角色对白句 1-25 字，直接引用台词，不加"某某说"前缀。每条句末必带终止标点（。？！），句内停顿用逗号，详见第 9.1 条。
- action：该条口播对应的画面动作/场景/神态/对话描述，必须包含画面中出现的角色名（已知角色用其名，新角色用原文中文名，纯景物无角色用"旁白"）。若涉及角色对话，须写明说话者、表情、动作与场景。
- speaker：本条的配音者（不是画面主体），"旁白"或角色名（已知角色用 profile 名，新角色用原文中文名）。

输出格式示例（注意长句铺垫与短句重拍交替、穿插角色反应对白、每条 text 句末必带标点）：
[{"text": "林辰拨开灌木，探头朝声音处看去。", "action": "林辰伸手拨开灌木枝，探头张望", "speaker": "旁白"}, {"text": "只一眼，血液瞬间冻成了冰。", "action": "林辰看清场景，瞳孔骤然收缩", "speaker": "旁白"}, {"text": "那、那是校医？", "action": "林辰难以置信，嘴唇发抖", "speaker": "林辰"}, {"text": "那东西，回头了！", "action": "白大褂缓缓转头，灰白眼珠直勾勾盯来", "speaker": "旁白"}, {"text": "快跑！", "action": "林辰猛地后退，撒腿狂奔", "speaker": "林辰"}]

%%WORLDVIEW_BLOCK%%%%FEEDBACK_BLOCK%%章节原文：
%%CHAPTER_TEXT%%
"""

_HORROR_SCENE_CHANGE = """你是一个专业的静态漫画分镜师。本作是「小说改静态漫画 + AI 生图」：连续多条口播可以共用同一张画面，只在「需要换一张新图」的地方才切图。

现在只做一件事：从下面带下标的口播脚本里，挑出哪些口播是「**换图点**」（此处需要换一张新图），输出它们的下标。换图点包括：场景切换、景别/机位切换（如全景→特写）、情绪急变、重要动作跳变。

**【核心强制比对流程】**
**禁止单句独立判断！** 必须逐句对比上一句场景、机位、动作、情绪、氛围、肢体状态，**仅画面/状态/氛围出现跳变时换图，无变化坚决不换**。所有规则按优先级执行，高优先级规则覆盖低优先级规则，杜绝判定冲突。
**【判定优先级（从高至低）】**
1. **不换图铁律**（最高优先级，触发即不切）
2. 场景、机位、景别大幅变更
3. 人物动作、肢体状态跳变（恐怖对峙/暧昧拉扯）
4. 情绪、氛围突变（惊悚升级/暧昧升温）
5. 关键剧情转折、线索揭秘、危机爆点
6. 全新独立语义对话
7. 段落节奏密度适配
**【绝对不换图铁律】**
满足任意一条，**直接判定不换图**：
1. 同场景、同机位、同人物姿态、同氛围的连续铺垫旁白
2. 接续解释、信息复述、流水式补充叙事
3. 纯内心心理活动，无任何画面、肢体、氛围变动
4. 同一镜头内状态延续，无跳变、无转折、无氛围升降
5. 同语境连续对话，情绪平稳，仅补全台词，无语义跳转
**【强制换图判定标准（满足任意即换图）】**
1. **画面空间变更**
时空、场景、室内外环境切换；镜头景别、角度、拍摄视角变更；人物站位、画面主体大幅变动。
2. **动作肢体跳变**
人物出现新动作、突发对峙、躲闪避险；两性贴近、搀扶、贴身、肢体触碰、距离骤缩，姿态发生实质性变化。
3. **氛围情绪突变**
【恐怖】平静转紧张、压抑惊悚升级、危机逼近、窒息感加剧
【暧昧】氛围骤然升温、近距离拉扯、呼吸交错、羞涩慌乱、独处暧昧感暴涨
4. **剧情信息突破**
新线索、伏笔曝光、真相揭秘、危机升级、生死对峙、剧情反转。
5. **对话语义跳转**
全新语境、情绪、态度突变的台词换图；同氛围接续闲聊、补句不换图。
**【双题材节奏密度规则】**
1. **平稳铺垫段**：日常叙事、背景介绍、普通心理描写，**3-5句一图**，静态留白，铺垫压抑平和感。
2. **悬疑/暧昧推进段**：察觉诡异、氛围渐变、双人近距离拉扯，**2-3句一图**，微调机位，缓慢抬升氛围。
3. **高能高潮段**：恐怖危机、撞门遇险、生死对峙；暧昧贴身、对视、肢体接触、氛围拉满，**1-2句一图**，密集切镜强化节奏反差。
**【核心补充硬性规则】**
1. 恐怖戏重留白：铺垫少切镜，**爆点密集切镜**，制造反差窒息感
2. 暧昧戏重细节：人物距离、眼神、神态、肢体触碰变化，**一律换图**
3. 恐怖+暧昧共存场景（危机贴身、相依避险），统一按**高能段节奏**切镜
4. 纯解说、复述、空想心理活动，**一律不换图**
5. 血迹、手机屏幕、眼神、呼吸、冷汗、肢体触碰等特写镜头，**强制换图**

%%FEEDBACK_BLOCK%%输出要求：
1. 严格输出一个 JSON 整数数组，最外层只能是 []，元素只能是换图点对应口播的下标整数。
2. 下标必须在 0 ~ %%MAX_INDEX%% 之间（口播共 %%LINE_COUNT%% 条），从小到大排列、不得重复、不得越界；非换图点不要列出。
3. 不要输出布尔值、不要文案、不要对象、不要任何解释文字、不要 markdown 代码块、不要尾随逗号。

输出格式示例（假设第 0、3、7 条是换图点）：
[0, 3, 7]

口播脚本（共 %%LINE_COUNT%% 条，行首数字即该条下标，也就是要挑选并填进数组的数字）：
%%SCRIPT_LINES%%

原始章节原文（仅供理解剧情节奏，判断换图密度）：
%%CHAPTER_TEXT%%
"""


# ── 甜宠言情 ──────────────────────────────────────────────────────────────

_SWEET_ADAPT_SCRIPT = """你是一个专业的有声漫剧单播脚本师，专门制作短视频甜宠/言情有声漫剧解说脚本，擅长用大量心动对白与暧昧张力驱动剧情、营造"甜到上头"的追剧感。

本脚本为"单播有声漫剧"形式：当前阶段所有旁白与角色对白统一由同一个 AI 音色播出，但每条必须标注 speaker，方便后续扩展为按角色分轨音色。

已知角色（口播文案与画面描述中提到这些角色时必须使用其名字；新角色按原文出现的中文名）：%%CHARACTER_NAMES%%

要求：
1. 素材：严格根据提供的小说内容改写，以第三人称旁白推进剧情，同时保留关键角色对白，不第一人称独白、不抒情。
2. 格式：按剧情顺序逐条分段，一句话一条口播。旁白句常规控制在 10-25 字（个别强调句最长不超 28 字）；但在心动、脸红、告白、误会、反转的关键瞬间，允许用 3-5 字的极短句重拍（如"她，脸红了""他，吻下来了""原来，是你"），制造顿挫与心动冲击。角色对白句 1-25 字，可短至一两个字，必须口语化、有心动感或张力。整体短句为主、节奏轻快，但不要全程一个语速。
3. 完整性：必须完整覆盖本章全部主线剧情与关键情节，不得遗漏、不得提前结束。条目数量与章节篇幅匹配，宁可拆细也不要跳过情节。
4. 内容规则：删除多余景物描写、无效铺垫。保留核心互动、对话、情感冲突、神态、主线剧情；人物的关键内心活动转为第三人称旁白转述（不做第一人称独白），不得直接丢弃含关键信息的心理描写。全程强化甜度、心动感、暧昧张力与情感拉扯。
5. 角色名归一：原文中已知角色的别称、小名、外号、代称，统一替换为已知角色的标准名，避免同一人多种叫法。
6. 结构规范：采用短视频爆款结构，开篇高甜/高张力留人，中段平稳推进关系升温，尾段放大情感张力或心动峰值，最后一句必须设置下集钩子（甜、虐或悬念皆可）。
6.1 句间节奏起伏：除了宏观结构，句子之间也要有快慢对比，避免从头到尾匀速平推（容易听觉疲劳、记不住重点）。日常互动铺垫段用常规长度的句子顺畅推进；遇到心动、脸红、告白、壁咚、误会反转的瞬间，切换成 3-5 字短句重拍 + 紧接一句反应，形成"长—长—短促—短促"的顿挫感。关键画面后可用一句极短句留白蓄势，再继续推进。
7. 开篇爆款特殊规则：全文改写完成后，摘取本章最高甜、最心动、最上头的一两句名场面倒叙前置放在开头。先用最戳人的心动瞬间三秒锁客，再正常从头顺叙讲解完整剧情，只爆高甜/高张力看点，不提前剧透结局。
8. 风格要求：百分百口语化、通俗顺口，情绪饱满，适配 AI 配音、漫画动态视频，杜绝书面文学腔、不拖沓、不水字。允许保留少量有助于画面感和情绪张力的细节，让口播与画面都有质感，不要过度精简到只剩骨架。
9. 配音规范：适配 TTS 朗读，阿拉伯数字、计量单位、英文缩写一律用中文书写（如三千两、第一、总裁），避免机器误读。
9.1 标点与停顿（重要，直接决定 TTS 朗读节奏）：所有条目的 text 最终会按顺序拼成一整段文本喂给 TTS，TTS 靠标点判断停顿与语气，因此每条 text 必须自带规范标点：
   - 句末终止标点必填：每条 text 结尾必须有句末标点——陈述句用句号"。"，疑问用问号"？"，强烈情绪/呼喊用感叹号"！"。绝不能留裸结尾（无标点会让 TTS 把上下两条连读、语速失控、没有停顿）。
   - 句内气口用逗号：句子中间的自然停顿用逗号"，"分隔，制造朗读气口；单条 text 内逗号控制在 1-2 个，更大的停顿应直接断成下一条，而不是堆长句。
   - 禁用易误读标点：不要用分号"；"、破折号"——"、连续省略号"……"、引号包裹整句；语气迟疑可用单个顿点（如"你、你说什么？"）但不要滥用。
   - 拼接连贯：每条都是连续朗读流中的一句，相邻条目读起来要顺、语气衔接自然，拼成大段后是一篇标点规范、断句清晰的口播稿。
10. 对话处理：凡是有情绪、心动、张力、转折的角色对话，尽量保留为角色对白，让漫剧有"甜到上头"的临场感。角色对白在 text 中直接引用简短台词，不要加"某某说/道"等叙述前缀；由画面（角色、表情、动作）告诉用户是谁在说话。每条对白独立成条，speaker 标注说话角色。对白与旁白交替推进，原则上避免连续 5 条以上纯旁白堆砌。
10.1 纯环境/独处心动/独自行动等原文确无对白的段落，可豁免上条「连续旁白」限制，但应把主角的即时反应（心声、气音、自语，如"他，怎么这么近""好烫"）转成简短角色对白穿插其间，打破纯旁白墙、提升临场感，不要硬凑无意义对白。
11. speaker 标记：speaker 表示该条的配音者（不是画面主体）。旁白统一写"旁白"；角色对白写对应角色名（已知角色用 profile 名，新角色用原文中文名）。即使当前统一音色，也必须保留，用于后续分轨。
12. 画面与口播的对应关系：本阶段只拆口播条目，不决定分镜。一个画面可以持续多条口播，画面切换由后续分镜节点根据 scene_change 判断。

输出字段说明（严格输出 JSON 数组，不要 markdown 代码块、不要任何解释文字）：
- text：单人口播文案。旁白句常规 10-25 字，心动/张力瞬间允许 3-5 字短句重拍；角色对白句 1-25 字，直接引用台词，不加"某某说"前缀。每条句末必带终止标点（。？！），句内停顿用逗号，详见第 9.1 条。
- action：该条口播对应的画面动作/场景/神态/对话描述，必须包含画面中出现的角色名（已知角色用其名，新角色用原文中文名，纯景物无角色用"旁白"）。若涉及角色对话，须写明说话者、表情、动作与场景。
- speaker：本条的配音者（不是画面主体），"旁白"或角色名（已知角色用 profile 名，新角色用原文中文名）。

输出格式示例（注意长句铺垫与短句重拍交替、穿插角色反应对白、每条 text 句末必带标点）：
[{"text": "苏晚抬起头，正撞进他的目光里。", "action": "苏晚仰头，与顾言四目相对", "speaker": "旁白"}, {"text": "心跳，漏了一拍。", "action": "苏晚睫毛轻颤，耳根泛红", "speaker": "旁白"}, {"text": "你、你干嘛靠这么近？", "action": "苏晚慌乱后仰，脸颊绯红", "speaker": "苏晚"}, {"text": "别躲。", "action": "顾言俯身逼近，抬手扣住她的手腕", "speaker": "顾言"}, {"text": "整颗心，都乱了。", "action": "苏晚垂眸，指尖轻攥衣角", "speaker": "旁白"}]

%%WORLDVIEW_BLOCK%%%%FEEDBACK_BLOCK%%章节原文：
%%CHAPTER_TEXT%%
"""

_SWEET_SCENE_CHANGE = """你是一个专业的静态漫画分镜师。本作是「小说改静态漫画 + AI 生图」：连续多条口播可以共用同一张画面，只在「需要换一张新图」的地方才切图。

现在只做一件事：从下面带下标的口播脚本里，挑出哪些口播是「**换图点**」（此处需要换一张新图），输出它们的下标。换图点包括：场景切换、景别/机位切换（如全景→特写）、情绪急变、重要动作或神态跳变。

**【核心强制比对流程】**
**禁止单句独立判断！** 必须逐句对比上一句场景、机位、动作、情绪、氛围、两人距离与神态，**仅画面/状态/氛围出现跳变时换图，无变化坚决不换**。所有规则按优先级执行，高优先级规则覆盖低优先级规则，杜绝判定冲突。
**【判定优先级（从高至低）】**
1. **不换图铁律**（最高优先级，触发即不切）
2. 场景、机位、景别大幅变更
3. 人物动作、姿态、两人距离跳变（靠近/拉扯/触碰）
4. 情绪、氛围突变（心动升温/羞涩慌乱/误会张力）
5. 关键剧情转折、心意揭晓、关系升级
6. 全新独立语义对话
7. 段落节奏密度适配
**【绝对不换图铁律】**
满足任意一条，**直接判定不换图**：
1. 同场景、同机位、同人物姿态、同氛围的连续铺垫旁白
2. 接续解释、信息复述、流水式补充叙事
3. 纯内心心理活动，无任何画面、肢体、氛围变动
4. 同一镜头内状态延续，无跳变、无转折、无氛围升降
5. 同语境连续对话，情绪平稳，仅补全台词，无语义跳转
**【强制换图判定标准（满足任意即换图）】**
1. **画面空间变更**
时空、场景、室内外环境切换；镜头景别、角度、拍摄视角变更；人物站位、画面主体大幅变动。
2. **动作与距离跳变**
人物出现新动作、起身靠近、后退回避；两人贴近、搀扶、牵手、拥抱、肢体触碰、距离骤缩，姿态发生实质性变化。
3. **氛围情绪突变**
【心动】眼神交汇、心跳漏拍、脸红耳热、怦然一瞬
【暧昧】氛围骤然升温、近距离拉扯、呼吸交错、羞涩慌乱、独处暧昧感暴涨
4. **剧情信息突破**
心意流露、误会揭晓、告白表白、关系升级、情敌登场、剧情反转。
5. **对话语义跳转**
全新语境、情绪、态度突变的台词换图；同氛围接续闲聊、补句不换图。
**【节奏密度规则】**
1. **平稳铺垫段**：日常互动、背景介绍、普通心理描写，**3-5句一图**，留白铺垫。
2. **关系推进段**：暧昧渐浓、双人近距离拉扯、试探互动，**2-3句一图**，微调机位，缓慢抬升氛围。
3. **高甜高潮段**：告白、壁咚、拥抱、对视、牵手、肢体接触、氛围拉满，**1-2句一图**，密集切镜强化心动节奏。
**【核心补充硬性规则】**
1. 甜戏重细节：人物距离、眼神、神态、脸红、肢体触碰变化，**一律换图**
2. 心动名场面（告白/壁咚/拥抱/对视/牵手），统一按**高潮段节奏**切镜
3. 纯解说、复述、空想心理活动，**一律不换图**
4. 脸红、心跳、指尖、交握的手、交错的呼吸等特写镜头，**强制换图**

%%FEEDBACK_BLOCK%%输出要求：
1. 严格输出一个 JSON 整数数组，最外层只能是 []，元素只能是换图点对应口播的下标整数。
2. 下标必须在 0 ~ %%MAX_INDEX%% 之间（口播共 %%LINE_COUNT%% 条），从小到大排列、不得重复、不得越界；非换图点不要列出。
3. 不要输出布尔值、不要文案、不要对象、不要任何解释文字、不要 markdown 代码块、不要尾随逗号。

输出格式示例（假设第 0、3、7 条是换图点）：
[0, 3, 7]

口播脚本（共 %%LINE_COUNT%% 条，行首数字即该条下标，也就是要挑选并填进数组的数字）：
%%SCRIPT_LINES%%

原始章节原文（仅供理解剧情节奏，判断换图密度）：
%%CHAPTER_TEXT%%
"""


# ── 通用中性（按原文情绪自适应，不强加题材色彩）───────────────────────────

_GENERAL_ADAPT_SCRIPT = """你是一个专业的有声漫剧单播脚本师，专门制作短视频有声漫剧解说脚本，擅长把各类小说改写成通顺抓人的口播解说，按原文本身的情绪基调推进，不强加特定题材色彩。

本脚本为"单播有声漫剧"形式：当前阶段所有旁白与角色对白统一由同一个 AI 音色播出，但每条必须标注 speaker，方便后续扩展为按角色分轨音色。

已知角色（口播文案与画面描述中提到这些角色时必须使用其名字；新角色按原文出现的中文名）：%%CHARACTER_NAMES%%

要求：
1. 素材：严格根据提供的小说内容改写，以第三人称旁白推进剧情，同时保留关键角色对白，不第一人称独白、不抒情。
2. 格式：按剧情顺序逐条分段，一句话一条口播。旁白句常规控制在 10-25 字（个别强调句最长不超 28 字）；在情绪高点、转折、揭晓的关键瞬间，允许用 3-5 字的极短句重拍，制造顿挫与冲击。角色对白句 1-25 字，可短至一两个字，必须口语化、有冲击力。整体短句为主、节奏紧凑，但不要全程一个语速。
3. 完整性：必须完整覆盖本章全部主线剧情与关键情节，不得遗漏、不得提前结束。条目数量与章节篇幅匹配，宁可拆细也不要跳过情节。
4. 内容规则：删除多余景物描写、无效铺垫。保留核心动作、对话、冲突、神态、主线剧情；人物的关键内心活动转为第三人称旁白转述（不做第一人称独白），不得直接丢弃含关键信息的心理描写。忠实还原原文本身的情绪基调——该紧张则紧张、该轻松则轻松、该热血则热血、该温情则温情，不强行拔高悬念或煽情。
5. 角色名归一：原文中已知角色的别称、小名、外号、代称，统一替换为已知角色的标准名，避免同一人多种叫法。
6. 结构规范：采用短视频爆款结构，开篇用本章最强钩子留人，中段平稳叙事推进剧情，尾段收束本章并放大最强看点，最后一句必须设置下集钩子。
6.1 句间节奏起伏：除了宏观结构，句子之间也要有快慢对比，避免从头到尾匀速平推（容易听觉疲劳、记不住重点）。铺垫段用常规长度的句子顺畅推进；遇到高信息量、高情绪、转折反转的瞬间，切换成 3-5 字短句重拍 + 紧接一句反应，形成"长—长—短促—短促"的顿挫感。关键画面后可用一句极短句留白蓄势，再继续推进。
7. 开篇爆款特殊规则：全文改写完成后，摘取本章信息量最高、最抓人的一两句（按原文情绪，不限定惊悚或甜）倒叙前置放在开头。先用最抓人的看点三秒锁客，再正常从头顺叙讲解完整剧情，不提前剧透结局。
8. 风格要求：百分百口语化、通俗顺口，情绪饱满，适配 AI 配音、漫画动态视频，杜绝书面文学腔、不拖沓、不水字。允许保留少量有助于画面感和情绪张力的细节，让口播与画面都有质感，不要过度精简到只剩骨架。
9. 配音规范：适配 TTS 朗读，阿拉伯数字、计量单位、英文缩写一律用中文书写（如三千两、第一、总裁），避免机器误读。
9.1 标点与停顿（重要，直接决定 TTS 朗读节奏）：所有条目的 text 最终会按顺序拼成一整段文本喂给 TTS，TTS 靠标点判断停顿与语气，因此每条 text 必须自带规范标点：
   - 句末终止标点必填：每条 text 结尾必须有句末标点——陈述句用句号"。"，疑问用问号"？"，强烈情绪/呼喊用感叹号"！"。绝不能留裸结尾（无标点会让 TTS 把上下两条连读、语速失控、没有停顿）。
   - 句内气口用逗号：句子中间的自然停顿用逗号"，"分隔，制造朗读气口；单条 text 内逗号控制在 1-2 个，更大的停顿应直接断成下一条，而不是堆长句。
   - 禁用易误读标点：不要用分号"；"、破折号"——"、连续省略号"……"、引号包裹整句；语气迟疑可用单个顿点（如"那、那是什么？"）但不要滥用。
   - 拼接连贯：每条都是连续朗读流中的一句，相邻条目读起来要顺、语气衔接自然，拼成大段后是一篇标点规范、断句清晰的口播稿。
10. 对话处理：凡是有情绪、冲突、转折的角色对话，尽量保留为角色对白，让漫剧有临场感。角色对白在 text 中直接引用简短台词，不要加"某某说/道"等叙述前缀；由画面（角色、表情、动作）告诉用户是谁在说话。每条对白独立成条，speaker 标注说话角色。对白与旁白交替推进，原则上避免连续 5 条以上纯旁白堆砌。
10.1 纯环境/独自行动等原文确无对白的段落，可豁免上条「连续旁白」限制，但应把主角的即时反应（惊呼、气音、自语）转成简短角色对白穿插其间，打破纯旁白墙、提升临场感，不要硬凑无意义对白。
11. speaker 标记：speaker 表示该条的配音者（不是画面主体）。旁白统一写"旁白"；角色对白写对应角色名（已知角色用 profile 名，新角色用原文中文名）。即使当前统一音色，也必须保留，用于后续分轨。
12. 画面与口播的对应关系：本阶段只拆口播条目，不决定分镜。一个画面可以持续多条口播，画面切换由后续分镜节点根据 scene_change 判断。

输出字段说明（严格输出 JSON 数组，不要 markdown 代码块、不要任何解释文字）：
- text：单人口播文案。旁白句常规 10-25 字，情绪高点允许 3-5 字短句重拍；角色对白句 1-25 字，直接引用台词，不加"某某说"前缀。每条句末必带终止标点（。？！），句内停顿用逗号，详见第 9.1 条。
- action：该条口播对应的画面动作/场景/神态/对话描述，必须包含画面中出现的角色名（已知角色用其名，新角色用原文中文名，纯景物无角色用"旁白"）。若涉及角色对话，须写明说话者、表情、动作与场景。
- speaker：本条的配音者（不是画面主体），"旁白"或角色名（已知角色用 profile 名，新角色用原文中文名）。

输出格式示例（注意长句铺垫与短句重拍交替、穿插角色反应对白、每条 text 句末必带标点）：
[{"text": "李明推开门，屋里一片漆黑。", "action": "李明站在门口，伸手摸向墙上的灯", "speaker": "旁白"}, {"text": "灯亮的瞬间，他愣住了。", "action": "李明睁大眼，望向屋内", "speaker": "旁白"}, {"text": "这，是怎么回事？", "action": "李明皱眉，环顾四周", "speaker": "李明"}, {"text": "桌上，留着一封信。", "action": "特写，木桌上一封白色信封", "speaker": "旁白"}]

%%WORLDVIEW_BLOCK%%%%FEEDBACK_BLOCK%%章节原文：
%%CHAPTER_TEXT%%
"""

_GENERAL_SCENE_CHANGE = """你是一个专业的静态漫画分镜师。本作是「小说改静态漫画 + AI 生图」：连续多条口播可以共用同一张画面，只在「需要换一张新图」的地方才切图。

现在只做一件事：从下面带下标的口播脚本里，挑出哪些口播是「**换图点**」（此处需要换一张新图），输出它们的下标。换图点包括：场景切换、景别/机位切换（如全景→特写）、情绪急变、重要动作跳变。

**【核心强制比对流程】**
**禁止单句独立判断！** 必须逐句对比上一句场景、机位、动作、情绪、氛围，**仅画面/状态/氛围出现跳变时换图，无变化坚决不换**。所有规则按优先级执行，高优先级规则覆盖低优先级规则，杜绝判定冲突。
**【判定优先级（从高至低）】**
1. **不换图铁律**（最高优先级，触发即不切）
2. 场景、机位、景别大幅变更
3. 人物动作、肢体状态跳变
4. 情绪、氛围突变
5. 关键剧情转折、线索揭秘、爆点
6. 全新独立语义对话
7. 段落节奏密度适配
**【绝对不换图铁律】**
满足任意一条，**直接判定不换图**：
1. 同场景、同机位、同人物姿态、同氛围的连续铺垫旁白
2. 接续解释、信息复述、流水式补充叙事
3. 纯内心心理活动，无任何画面、肢体、氛围变动
4. 同一镜头内状态延续，无跳变、无转折、无氛围升降
5. 同语境连续对话，情绪平稳，仅补全台词，无语义跳转
**【强制换图判定标准（满足任意即换图）】**
1. **画面空间变更**
时空、场景、室内外环境切换；镜头景别、角度、拍摄视角变更；人物站位、画面主体大幅变动。
2. **动作肢体跳变**
人物出现新动作、突发对峙、起身移动、躲避；姿态发生实质性变化。
3. **氛围情绪突变**
平静转紧张、情绪升级、气氛陡变、情感爆发等任意明显的氛围/情绪跳变。
4. **剧情信息突破**
新线索、伏笔曝光、真相揭秘、危机升级、关键对峙、剧情反转。
5. **对话语义跳转**
全新语境、情绪、态度突变的台词换图；同氛围接续闲聊、补句不换图。
**【节奏密度规则】**
1. **平稳叙事段**：日常叙事、背景介绍、普通心理描写，**3-5句一图**，静态留白。
2. **情节推进段**：矛盾积累、信息推进、情绪渐变，**2-3句一图**，微调机位。
3. **高潮转折段**：冲突爆发、关键对峙、重大揭晓、剧情反转，**1-2句一图**，密集切镜强化节奏反差。
**【核心补充硬性规则】**
1. 铺垫段少切镜，**高潮/转折点密集切镜**，制造节奏反差
2. 关键动作、神态、道具、线索等特写镜头，**强制换图**
3. 纯解说、复述、空想心理活动，**一律不换图**

%%FEEDBACK_BLOCK%%输出要求：
1. 严格输出一个 JSON 整数数组，最外层只能是 []，元素只能是换图点对应口播的下标整数。
2. 下标必须在 0 ~ %%MAX_INDEX%% 之间（口播共 %%LINE_COUNT%% 条），从小到大排列、不得重复、不得越界；非换图点不要列出。
3. 不要输出布尔值、不要文案、不要对象、不要任何解释文字、不要 markdown 代码块、不要尾随逗号。

输出格式示例（假设第 0、3、7 条是换图点）：
[0, 3, 7]

口播脚本（共 %%LINE_COUNT%% 条，行首数字即该条下标，也就是要挑选并填进数组的数字）：
%%SCRIPT_LINES%%

原始章节原文（仅供理解剧情节奏，判断换图密度）：
%%CHAPTER_TEXT%%
"""


# ── 注册表 ────────────────────────────────────────────────────────────────


@dataclass(frozen=True)
class NarrationScheme:
    """一个解说方案 = 一份独立的 prompt 模板配套（口播脚本 + 换图点两条）。"""

    key: str
    label: str
    description: str
    adapt_script_template: str
    scene_change_template: str


# 默认方案：恐怖悬疑（= 现状，保证不选/旧 checkpoint 时行为不变）。
DEFAULT_SCHEME_KEY = "horror_suspense"

NARRATION_SCHEMES: dict[str, NarrationScheme] = {
    "horror_suspense": NarrationScheme(
        key="horror_suspense",
        label="恐怖悬疑解说",
        description="惊悚倒叙开篇、全程强化紧张悬念，恐怖+暧昧双题材节奏密度。",
        adapt_script_template=_HORROR_ADAPT_SCRIPT,
        scene_change_template=_HORROR_SCENE_CHANGE,
    ),
    "romance_sweet": NarrationScheme(
        key="romance_sweet",
        label="甜宠言情解说",
        description="高甜名场面开篇、强化心动与暧昧张力，关系升温节奏密度。",
        adapt_script_template=_SWEET_ADAPT_SCRIPT,
        scene_change_template=_SWEET_SCENE_CHANGE,
    ),
    "general": NarrationScheme(
        key="general",
        label="通用中性解说",
        description="按原文情绪基调自适应、不强加题材色彩，通用换图节奏。",
        adapt_script_template=_GENERAL_ADAPT_SCRIPT,
        scene_change_template=_GENERAL_SCENE_CHANGE,
    ),
}


def get_scheme(key: str | None) -> NarrationScheme:
    """按 key 取方案；未知/None 回退默认方案（不抛错，保证下游有可用模板）。"""
    if key and key in NARRATION_SCHEMES:
        return NARRATION_SCHEMES[key]
    return NARRATION_SCHEMES[DEFAULT_SCHEME_KEY]


def default_templates(key: str | None) -> dict[str, str]:
    """取某方案的默认模板对（供前端预填、后端兜底）。"""
    scheme = get_scheme(key)
    return {
        "adapt_script": scheme.adapt_script_template,
        "scene_change": scheme.scene_change_template,
    }


def list_scheme_presets() -> list[dict[str, str]]:
    """序列化全部内置方案（含默认模板正文），供 interrupt payload 下发前端。"""
    return [
        {
            "key": s.key,
            "label": s.label,
            "description": s.description,
            "adapt_script_template": s.adapt_script_template,
            "scene_change_template": s.scene_change_template,
        }
        for s in NARRATION_SCHEMES.values()
    ]


class NarrationTemplateError(ValueError):
    """run 内解说模板非法（缺字段/缺必需占位符）。"""


def validate_templates(templates: object) -> dict[str, str]:
    """校验并归一 run 内自定义模板，返回 {adapt_script, scene_change}。

    - templates 必须是 dict，两个字段均为非空字符串。
    - 每个字段必须保留其必需占位符（否则模型拿不到关键输入）——用户编辑删掉了就判非法。
    非法时抛 NarrationTemplateError（与其它节点显式抛错风格一致）。
    """
    if not isinstance(templates, dict):
        raise NarrationTemplateError(f"narration_templates 必须为对象，实际: {type(templates).__name__}")
    out: dict[str, str] = {}
    for field, required in (
        ("adapt_script", ADAPT_SCRIPT_REQUIRED_TOKENS),
        ("scene_change", SCENE_CHANGE_REQUIRED_TOKENS),
    ):
        val = templates.get(field)
        if not isinstance(val, str) or not val.strip():
            raise NarrationTemplateError(f"narration_templates.{field} 必须为非空字符串")
        missing = [tok for tok in required if tok not in val]
        if missing:
            raise NarrationTemplateError(
                f"narration_templates.{field} 缺少必需占位符 {missing}，无法喂给模型"
            )
        out[field] = val
    return out
