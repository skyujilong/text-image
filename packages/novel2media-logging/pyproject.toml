[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "novel2media-logging"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "structlog>=24.1.0",
]
